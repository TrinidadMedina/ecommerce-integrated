/* const socket = io();

const formCreateUser = document.getElementById('formCreateUser');

let authorEmail = '';

formCreateAuthor.addEventListener('submit', e => {
    e.preventDefault()
    const userName: formCreateUser[0].value;
    formCreateUser.reset();
    //SESSION
});   
 */